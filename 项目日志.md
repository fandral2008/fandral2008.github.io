# 项目日志

## 2026-08-31

- 从本地家庭通信项目的实机验收记录中提取公开边界，新增“家庭强触达通信”私有工程案例，不公开私有仓库、家庭 IP、设备身份、凭据或内部截图。
- 页面只把普通振铃、双向语音、提示后自动接听与物理静音列为已真机验证；“渐强铃声”明确标为下一轮设计验证，未冒充现有终端能力。
- 项目总数改为由前端从真实条目数量计算，新增桌面与移动端响应式案例布局。
- 浏览器验收开始时再次调用当前后端不支持的 `networkidle`，该等待在页面判定前失败，不能记为页面故障或加载结果；已停止重复调用并改用 `domcontentloaded + 目标标题 visible` 的可判定业务信号。后续同类静态页禁止再以 `networkidle` 作为首选等待条件。
- 修正等待条件后，真实浏览器在 1440×1000 与 390×844 下均为横向溢出 0；项目数为 7，“医疗影像”筛选为 2、“学习工具”筛选为 1，恢复“全部”后为 7；当前新标签控制台 error/warn 为 0。
- 页面更新提交 `7e4302b055179caefc5d6febf693e161247feb78` 已推送到 `main`；GitHub Pages 工作流 `33372287849` 的 Checkout、Configure、Upload 与 Deploy 全部成功。公网 `https://fandral2008.github.io/?v=7e4302b` 已回读到新增标题、7 个项目和“渐强铃声 · 设计验证中”，桌面/移动端横向溢出均为 0，线上新标签控制台 error/warn 为 0。
- 最终状态查询更正：首次将工作流名猜成 `pages.yml`，`gh run list` 返回 404，该结果仅说明查询入口不存在，不能用于判定发布失败。已从仓库冻结真实入口 `.github/workflows/deploy-pages.yml`；后续查询必须使用实读文件名或工作流 ID。
- 公网 HTML 回读更正：首次用不存在的精确字面 `<article class="project-card">` 计数，将真实存在的 7 个 `<article class="project ...">` 误报为 0；随后又把四项业务能力猜成一句连续文本，而页面实际文案是“普通振铃、双向语音、提示后自动接听与物理静音”，再次产生假阴性。两次断言都只说明探针与实际 DOM/文案不一致，不能判定页面缺失；后续必须用 HTML 解析器按 `article[data-category]` 结构计数，并将业务能力逐项独立断言。

## 2026-08-16

- 建立统一个人项目展厅首版。
- 完成英雄区、项目筛选轨道、方法、关于与页脚。
- 接入理清 Clarify 在线体验和源码，以及 WeChat Radar 源码入口。
- 私有项目仅保留能力描述和状态，不暴露私有仓库。
- 增加 GitHub Pages Actions 工作流与公开内容边界说明。
- 浏览器首轮验收发现桌面主标题断行为三行，已调整为概念规定的两行构图。
- 通过桌面 1440×1000、移动端 390×844 浏览器验收；控制台无警告或错误。
- 验证“医疗影像”筛选：计数由 6 更新为 2，只保留 Radiology Guard 与 Technician Copilot。
- 将英雄区透明线稿压缩为 WebP，并移除不参与发布的概念与 QA 临时图。
- 增加独立 favicon，消除本地服务器的缺失资源请求。

### 更正

- 首次终检脚本在 zsh 循环中误用变量名 `path`；zsh 将其与 `PATH` 关联，导致同一 shell 后续出现 `curl/rg/git: command not found`。该轮命令输出作废，改用任务专用变量名重新执行完整终检。
- 更正后完整重跑终检：HTML 本地引用 5 项全部存在；首页、CSS、JS、两张图像与 favicon 均返回 200；4 个公开外链均返回 200。
- 初始化本地 `main` Git 仓库。
- 创建公开远程仓库 `fandral2008/fandral2008.github.io`；创建时为空仓库，尚未提交或推送。
- 首次提交 `cc7d93b` 已推送至 `main`。
- 通过 GitHub Pages REST API 启用 `workflow` 发布模式与 HTTPS；Actions 运行 `31930450505` 成功完成。
- 公网 `https://fandral2008.github.io/` 及 CSS、JS、两张核心图像均返回 200；线上浏览器标题、DOM 与控制台检查通过。
- Actions 对部分官方 Pages action 给出 Node.js 20 弃用提示，当前由 GitHub 自动强制使用 Node.js 24，未影响部署。
